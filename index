<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Trò chơi chọn món ăn</title>
  <style>
    body { 
      font-family: Arial, sans-serif; 
      text-align: center; 
      margin: 0; 
      padding: 20px; 
      background: #fff5f5;
    }
    h2 { color: #d6336c; }
    .choices {
      display: flex;
      justify-content: center;
      gap: 20px;
      margin-top: 30px;
      flex-wrap: wrap;
    }
    .choice {
      width: 150px;
      height: 150px;
      border: 3px solid #ff4d6d;
      border-radius: 15px;
      overflow: hidden;
      cursor: pointer;
      transition: transform 0.3s, box-shadow 0.3s;
    }
    .choice img {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }
    .choice:hover {
      transform: scale(1.1);
      box-shadow: 0 0 15px rgba(255,0,100,0.5);
    }
    #result {
      margin-top: 30px;
      font-size: 20px;
      color: #28a745;
    }
  </style>
</head>
<body>
  <h2>Em chọn món nào ngon hơn? 🍽️</h2>
  <div class="choices" id="choices"></div>
  <h3 id="result"></h3>

  <script>
    // Danh sách món ăn (tên + link ảnh)
    const foods = [
      {name: "Pizza", img: "https://i.ibb.co/r22fS9K/pizza.jpg"},
      {name: "Sushi", img: "https://i.ibb.co/hMWnt7b/sushi.jpg"},
      {name: "Lẩu", img: "https://i.ibb.co/yQtXZxH/hotpot.jpg"},
      {name: "Bún chả", img: "https://i.ibb.co/sCPbznT/bun-cha.jpg"},
      {name: "Hamburger", img: "https://i.ibb.co/cC2cwBS/burger.jpg"},
      {name: "Taco", img: "https://i.ibb.co/1nnKkfc/taco.jpg"},
      {name: "Cơm tấm", img: "https://i.ibb.co/3CnbvLq/com-tam.jpg"}
    ];
    
    let currentIndex = 1;
    let currentWinner = foods[0];

    function showChoices() {
      if (currentIndex >= foods.length) {
        document.getElementById("choices").innerHTML = "";
        document.getElementById("result").innerText = 
          "🎉 Món em chọn cuối cùng là: " + currentWinner.name + " 🎉";
        return;
      }

      const challenger = foods[currentIndex];
      const choicesDiv = document.getElementById("choices");
      choicesDiv.innerHTML = `
        <div class="choice" onclick="pick('${currentWinner.name}')">
          <img src="${currentWinner.img}" alt="${currentWinner.name}">
        </div>
        <div class="choice" onclick="pick('${challenger.name}')">
          <img src="${challenger.img}" alt="${challenger.name}">
        </div>
      `;
    }

    function pick(choiceName) {
      // tìm object theo tên
      currentWinner = foods.find(f => f.name === choiceName);
      currentIndex++;
      showChoices();
    }

    showChoices();
  </script>
</body>
</html>
